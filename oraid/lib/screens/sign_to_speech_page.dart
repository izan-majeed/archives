import 'package:flutter/material.dart';

class SignToSpeechPage extends StatelessWidget {
  static const String id = 'SignToSpeechPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This feature will be available soon'),
      ),
    );
  }
}
